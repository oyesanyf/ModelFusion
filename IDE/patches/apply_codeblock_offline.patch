diff --git a/extensions/copilot/src/extension/byok/vscode-node/evolve/inlineDiff.ts b/extensions/copilot/src/extension/byok/vscode-node/evolve/inlineDiff.ts
index 100a5692..2fa7a821 100644
--- a/extensions/copilot/src/extension/byok/vscode-node/evolve/inlineDiff.ts
+++ b/extensions/copilot/src/extension/byok/vscode-node/evolve/inlineDiff.ts
@@ -18,6 +18,8 @@ export interface PendingEdit {
 	statusBarItem: vscode.StatusBarItem;
 	/** Disposable for the auto-dismiss listener. */
 	autoDismiss?: vscode.Disposable;
+	/** Disposable for the save listener (Ctrl+S). */
+	saveListener?: vscode.Disposable;
 }
 
 /**
@@ -227,6 +229,14 @@ export class InlineDiffManager {
 		});
 		this._pendingEdit.autoDismiss = autoDismiss;
 
+		// Auto-accept when user saves the file (e.g. Ctrl+S or File -> Save)
+		const saveListener = vscode.workspace.onDidSaveTextDocument((savedDoc) => {
+			if (this._pendingEdit && savedDoc.uri.toString() === this._pendingEdit.editor.document.uri.toString()) {
+				this.accept();
+			}
+		});
+		this._pendingEdit.saveListener = saveListener;
+
 		// Notification toast with both actions
 		const changeCount = addedRanges.length + removedRanges.length;
 		vscode.window.showInformationMessage(
@@ -246,17 +256,18 @@ export class InlineDiffManager {
 	 */
 	async accept(): Promise<void> {
 		if (!this._pendingEdit) { return; }
-		const { editor, statusBarItem, autoDismiss } = this._pendingEdit;
+		const { editor, statusBarItem, autoDismiss, saveListener } = this._pendingEdit;
+
+		// Clear state and listeners first so onDidSaveTextDocument does not re-trigger accept
+		this._pendingEdit = null;
+		autoDismiss?.dispose();
+		saveListener?.dispose();
+		statusBarItem.dispose();
+		this._clearDecorations(editor);
 
 		// Save the file (proposed code is already applied)
 		await editor.document.save();
 
-		// Clear all decorations
-		this._clearDecorations(editor);
-		statusBarItem.dispose();
-		autoDismiss?.dispose();
-		this._pendingEdit = null;
-
 		vscode.window.showInformationMessage('✅ Changes accepted and saved!');
 	}
 
@@ -265,7 +276,12 @@ export class InlineDiffManager {
 	 */
 	async reject(): Promise<void> {
 		if (!this._pendingEdit) { return; }
-		const { editor, originalCode, statusBarItem, autoDismiss } = this._pendingEdit;
+		const { editor, originalCode, statusBarItem, autoDismiss, saveListener } = this._pendingEdit;
+
+		this._pendingEdit = null;
+		autoDismiss?.dispose();
+		saveListener?.dispose();
+		statusBarItem.dispose();
 
 		// Revert to original code
 		const fullRange = new vscode.Range(
@@ -278,9 +294,6 @@ export class InlineDiffManager {
 
 		// Clear all decorations
 		this._clearDecorations(editor);
-		statusBarItem.dispose();
-		autoDismiss?.dispose();
-		this._pendingEdit = null;
 
 		vscode.window.showInformationMessage('❌ Changes rejected. Original code restored.');
 	}
diff --git a/extensions/copilot/src/extension/conversation/node/aiMappedEditsProvider.ts b/extensions/copilot/src/extension/conversation/node/aiMappedEditsProvider.ts
index 7234d6c5..26e63bd8 100644
--- a/extensions/copilot/src/extension/conversation/node/aiMappedEditsProvider.ts
+++ b/extensions/copilot/src/extension/conversation/node/aiMappedEditsProvider.ts
@@ -2,7 +2,7 @@
  *  Copyright (c) Microsoft Corporation. All rights reserved.
  *  Licensed under the MIT License. See License.txt in the project root for license information.
  *--------------------------------------------------------------------------------------------*/
-import type * as vscode from 'vscode';
+import * as vscode from 'vscode';
 import { ICodeMapperService } from '../../prompts/node/codeMapper/codeMapperService';
 
 export class AIMappedEditsProvider2 implements vscode.MappedEditsProvider2 {
@@ -25,16 +25,37 @@ export class AIMappedEditsProvider2 implements vscode.MappedEditsProvider2 {
 				return undefined;
 			}
 
-			const result = await this._codeMapperService.mapCode({ codeBlock, location: request.location }, response, {
-				isAgent: request.location === 'tool',
-				chatRequestId: request.chatRequestId,
-				chatSessionId: request.chatSessionId,
-				chatRequestSource: `api_${request.location}`,
-				chatRequestModel: request.chatRequestModel,
-			}, token);
-			if (result) {
-				if (result.errorDetails) {
-					errorMessages.push(result.errorDetails.message);
+			let hadError = false;
+			try {
+				const result = await this._codeMapperService.mapCode({ codeBlock, location: request.location }, response, {
+					isAgent: request.location === 'tool',
+					chatRequestId: request.chatRequestId,
+					chatSessionId: request.chatSessionId,
+					chatRequestSource: `api_${request.location}`,
+					chatRequestModel: request.chatRequestModel,
+				}, token);
+				if (result) {
+					if (result.errorDetails) {
+						hadError = true;
+						errorMessages.push(result.errorDetails.message);
+					}
+				}
+			} catch (e: any) {
+				hadError = true;
+				errorMessages.push(e?.message ?? String(e));
+			}
+
+			// HugOS Fallback: If cloud code mapper fails (e.g. offline, key is missing),
+			// directly apply the code block to the target document as a full-document replacement edit!
+			// This ensures "Apply in Editor" works offline and presents the native inline diff review.
+			if (hadError && codeBlock?.resource && codeBlock?.code) {
+				try {
+					const doc = await vscode.workspace.openTextDocument(codeBlock.resource);
+					const fullRange = new vscode.Range(0, 0, doc.lineCount, doc.lineAt(Math.max(0, doc.lineCount - 1)).text.length);
+					response.textEdit(codeBlock.resource, [new vscode.TextEdit(fullRange, codeBlock.code)]);
+					errorMessages.length = 0;
+				} catch (docErr) {
+					// keep original error if document cannot be opened
 				}
 			}
 		}
diff --git a/src/vs/workbench/contrib/chat/browser/actions/codeBlockOperations.ts b/src/vs/workbench/contrib/chat/browser/actions/codeBlockOperations.ts
index c3650b84..e1bca267 100644
--- a/src/vs/workbench/contrib/chat/browser/actions/codeBlockOperations.ts
+++ b/src/vs/workbench/contrib/chat/browser/actions/codeBlockOperations.ts
@@ -307,12 +307,25 @@ export class ApplyCodeBlockOperation {
 		const codeBlock = { code, resource: activeModel.uri, chatSessionResource, markdownBeforeBlock: undefined };
 
 		const codeMapper = this.codeMapperService.providers[0]?.displayName;
-		if (!codeMapper) {
-			this.notify(localize('applyCodeBlock.noCodeMapper', "No code mapper available."));
-			return undefined;
-		}
 		let editsProposed = false;
 		const cancellationTokenSource = new CancellationTokenSource();
+
+		if (!codeMapper) {
+			// HugOS fallback: No code mapper registered, apply directly as inline preview
+			try {
+				const fallbackRange = activeModel.getFullModelRange();
+				const fallbackEdits: TextEdit[] = [{ range: fallbackRange, text: code }];
+				const fallbackIterable: AsyncIterable<TextEdit[]> = (async function* () {
+					yield fallbackEdits;
+				})();
+				editsProposed = await this.applyWithInlinePreview(fallbackIterable, codeEditor, cancellationTokenSource, applyCodeBlockSuggestionId);
+				return { editsProposed, codeMapper: 'local' };
+			} catch (fallbackErr) {
+				this.notify(localize('applyCodeBlock.noCodeMapper', "No code mapper available."));
+				return undefined;
+			}
+		}
+
 		try {
 			const iterable = await this.progressService.withProgress<AsyncIterable<TextEdit[]>>(
 				{ location: ProgressLocation.Notification, delay: 500, sticky: true, cancellable: true },
@@ -326,7 +339,18 @@ export class ApplyCodeBlockOperation {
 			editsProposed = await this.applyWithInlinePreview(iterable, codeEditor, cancellationTokenSource, applyCodeBlockSuggestionId);
 		} catch (e) {
 			if (!isCancellationError(e)) {
-				this.notify(localize('applyCodeBlock.error', "Failed to apply code block: {0}", e.message));
+				// HugOS fallback: If codeMapper fails or throws (offline/key missing),
+				// apply the code directly to the active editor with inline preview.
+				try {
+					const fallbackRange = activeModel.getFullModelRange();
+					const fallbackEdits: TextEdit[] = [{ range: fallbackRange, text: code }];
+					const fallbackIterable: AsyncIterable<TextEdit[]> = (async function* () {
+						yield fallbackEdits;
+					})();
+					editsProposed = await this.applyWithInlinePreview(fallbackIterable, codeEditor, cancellationTokenSource, applyCodeBlockSuggestionId);
+				} catch (fallbackErr) {
+					this.notify(localize('applyCodeBlock.error', "Failed to apply code block: {0}", e.message));
+				}
 			}
 		} finally {
 			cancellationTokenSource.dispose();
