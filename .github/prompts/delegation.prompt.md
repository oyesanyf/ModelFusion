---
name: delegation
description: Enable task delegation to sub-models
---
# Task Delegation

Enables task delegation where complex tasks are distributed across specialized sub-models.

## Usage
- Splits compound tasks into subtasks matched to optimal models
- Routes NLP, vision, and code tasks to domain-specific models
- Aggregates results from sub-models into a unified response
- Reduces latency by running independent subtasks in parallel
